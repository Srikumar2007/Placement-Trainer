{
    "name": "skillforge-project",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix frontend -- --host 127.0.0.1 --port 5173",
        "install-all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
        "backend": "cd backend && venv\\Scripts\\uvicorn main:app --reload --host 127.0.0.1 --port 5173",
        "frontend": "npm run dev --prefix frontend -- --host 127.0.0.1 --port 5173"
    }
}